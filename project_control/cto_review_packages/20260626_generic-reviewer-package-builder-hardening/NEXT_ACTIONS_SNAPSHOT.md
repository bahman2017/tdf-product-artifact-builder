# Next actions

1. Complete generic reviewer package builder hardening (current task)
2. CTO review of reviewer package builder branch and bundle
3. Open PR after CTO approval
4. Integrate with `tdf-openmm-validation` outputs (blocked until builder hardening stable)
5. External reviewer package generation from live diagnostic outputs (blocked)
6. Generate first review package from `lithium_filter_candidate_v0_1` (blocked until CTO approval)

Real product package generation remains blocked until generic builder hardening + CTO review are complete.
